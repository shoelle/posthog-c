# Zig build outputs
zig-out/
.zig-cache/
zig-cache/
node_modules/

# Object files / libraries / executables
*.o
*.obj
*.a
*.lib
*.exe
*.pdb
*.dll
*.so
*.so.*
*.dylib

# Editor / OS cruft
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Claude Code local state
.claude/

# Offline-queue test scratch dir (when TEMP is unset it falls back to cwd)
ph_offline_test/

# WASM test build outputs (emitted next to the harness by `zig build test-wasm`)
tests/wasm/test_wasm.mjs
tests/wasm/test_wasm.wasm
tests/wasm/test_wasm_closure.mjs
tests/wasm/test_wasm_closure.wasm

# Live-contract credentials - never commit real keys (.env.example is the template)
.env
.env.*
!.env.example
